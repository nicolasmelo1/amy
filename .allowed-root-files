# Files allowed at the repository root. Adding a line here is the
# deliberate act; NOTES.md appearing without one is the reflex.
# The pending version bumps, one file per change, consumed by
# `changeset version`. Empty between releases, and that is not a mistake.
.changeset
.env.example
.githooks
.github
.gitignore
.npmrc
.software-factory
CONTRIBUTING.md
LICENSE
README.md
docs
eslint.config.js
package-lock.json
package.json
plans
tsconfig.json
# The tests, type-checked. `tsconfig.json` builds `src` only, and vitest
# strips types without checking them, so without this a test can name a field
# that does not exist and stay green.
tsconfig.tests.json
vitest.config.ts
packages
plugins
# Builds and installs the executable. At the root because it is about the
# whole workspace, not about one package in it.
scripts
tsconfig.base.json
