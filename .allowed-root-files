# Files allowed at the repository root. Adding a line here is the
# deliberate act; NOTES.md appearing without one is the reflex.
# The pending version bumps, one file per change, consumed by
# `changeset version`. Empty between releases, and that is not a mistake.
.changeset
# The skills that belong to this repository rather than to anybody who
# installs amy. `/amy-develop` is here and not in `packages/cli/skills`
# because nothing about changing amy's own codebase travels with the command.
.claude
.env.example
.githooks
.github
.gitignore
.npmrc
.software-factory
LICENSE
README.md
docs
eslint.config.js
package-lock.json
package.json
plans
tsconfig.json
vitest.config.ts
packages
plugins
# Builds and installs the executable. At the root because it is about the
# whole workspace, not about one package in it.
scripts
tsconfig.base.json
